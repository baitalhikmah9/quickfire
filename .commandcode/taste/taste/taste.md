# Taste
- Prefers driving store/release work through CLI tools (e.g., `asc` for App Store Connect) instead of the web UI. Confidence: 0.8
- Prefers using MCP servers for third-party services (RevenueCat, App Store Connect) and proactively asks whether a service has an MCP or CLI before falling back to manual/browser work; uses browser automation for web-only setup when no MCP/CLI exists. Confidence: 0.8
- Wants end-to-end setup verified before acting — e.g., check that code, external services, and CLI config are all correctly wired before proceeding with builds/submissions. Confidence: 0.8
- Handles manual login/auth steps himself and reports back ("i've logged into revenuecat", "done"), expecting the agent to continue from that point rather than doing auth itself. Confidence: 0.7
- Tests on a real phone and expects the agent to debug device-reported errors, including driving the phone via mobile tools when asked. Confidence: 0.8
- Installs Android debug builds on a physical device via adb, built with sandbox (dev) env vars baked in (Expo `EXPO_PUBLIC_*` keys: Clerk test key, Convex dev instance, RevenueCat test store keys). Confidence: 0.7
- Attaches/invokes the strategic-compact skill to compact context at phase boundaries during long sessions. Confidence: 0.5
- Wants commits/pushes that exclude secrets and other inappropriate content ("don't include things that are inappropriate"). Confidence: 0.7
- Keeps final control over irreversible actions: wants a readiness check and an explicit go-ahead before submitting to App Review ("Let's submit."). Confidence: 0.7
- Issues terse, imperative system-maintenance commands (e.g., "kill all android processes and eject ssd") and expects the agent to autonomously investigate (check running processes, find what's holding the drive), resolve blockers (e.g., force-kill holding processes), and complete the task without step-by-step confirmation — responds "continue" to keep the agent moving. Confidence: 0.6
- Iterates in small increments and commits after each approved change, usually with a terse "commit changes"; often follows up with "commit and push" once a batch is done. Confidence: 0.85
- Uses the Himalaya CLI for email and keeps `mikhailspeaks` (mikhailspeaks@gmail.com) configured as the default account; refers to mailboxes by name and expects the agent to search/read the mailbox directly (Inbox and `[Gmail]/All Mail`), e.g. "check mikhailspeaks email through himalaya". Confidence: 0.6
- When locating a file/link someone shared, expects the agent to check multiple channels (WhatsApp chats and email) rather than treating one source as exhaustive. Confidence: 0.5
- Prefers the PinchTab browser tool to open/read web links (e.g. "use pinchtab browser to open the link") rather than fetching pages another way. Confidence: 0.6
- Wants `mikhailspeaks` (mikhailspeaks@gmail.com) used as the default account for any site/service login the agent encounters. Confidence: 0.6
- Tracks expected quantities and challenges reported numbers that don't match ("i thought there was 82 pngs? why only 78 staged?"); wants the agent to state verified, fully-contextualized counts up front (e.g., "82 files: 78 PNG + 4 WebP") rather than partial figures that invite confusion. Confidence: 0.5
- For question/data spreadsheets (Google Sheet "new gen knowledge qs"), wants each tab grouped by Topic and sorted Easy → Medium → Hard within each topic. Confidence: 0.8
- When updating a Google Sheets tab from a CSV, imports it at A1 with File > Import location "Replace data at selected cell" (preserves formatting) and never uses "Replace current sheet", which wipes cell formatting and the sheet's Table object. Confidence: 0.85
- After a row-count change in a spreadsheet, resets the Table range via the Table menu > "Adjust table range" so it matches the data instead of trailing empty rows. Confidence: 0.75
- Prefers maintaining Google Sheets via the `gws` (googleworkspace/cli) Sheets API instead of browser automation (pinchtab). Confidence: 0.85
- Falls back to Google Sheets version history to restore the design/formatting when an update has damaged it. Confidence: 0.6
- Wants a display-name/label change propagated consistently across every layer that carries it (UI copy, constants/data files, Convex seeds and translations, section-grouping maps, tests, and generator/import scripts), not just in the source spreadsheet, so the app itself shows the new name. Confidence: 0.55
- Wants recurring/project-specific workflows codified as reusable artifacts rather than re-derived each time: asks to "save this workflow" and to "insert a skill file in this repo", i.e. a checked-in repo-local skill (`.agents/skills/<name>/` with SKILL.md plus references/scripts) alongside the existing project skills. Confidence: 0.75
- Edits the shared question spreadsheet by hand between sessions and then hands it back ("i've added more questions. fix the formatting"), expecting the agent to reconcile the manual edits to the tab's conventions — re-sort rows, strip stray whitespace, restore missing cell values from the source CSV, and extend conditional-formatting/table ranges to the new last row — and to report any gaps it could not fill rather than guessing. Confidence: 0.55
- Expects a new/updated tab to mirror the conventions of the existing reference tab (same topic→difficulty ordering and the same difficulty colours), discovered by inspecting the reference tab first. Confidence: 0.5
- Expects spreadsheet formatting to be verified programmatically rather than by screenshot: read a cell's fill color from the toolbar Fill color button's swatch (child element's `border-bottom-color` style) and its value from the formula bar textbox; set an exact color via the color picker's CUSTOM > Add a custom color > Hex field, typing the hex then clicking OK (pressing Enter in the field does not commit). Confidence: 0.6
