import homeSoftUi from './home-soft-ui.json';

export type HomeSoftUiTheme = typeof homeSoftUi;

/** JSON design tokens for the neumorphic home hub (import in `app/(app)/index.tsx`). */
export const HOME_SOFT_UI: HomeSoftUiTheme = homeSoftUi;
